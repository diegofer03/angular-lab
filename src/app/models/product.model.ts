import { category } from "./category.model"

export interface product {
  id: 4,
    title: string,
    price: number,
    description: string,
    category: category
    images: string[]
    updatedAt: string
}
